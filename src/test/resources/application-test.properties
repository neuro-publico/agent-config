# Test configuration - uses test resources (Testcontainers)
micronaut.application.name=agent-config-ms-test

# Disable flyway for unit tests that don't need DB
flyway.datasources.default.enabled=true

# Vector DB mock URL (not used in tests with mocks)
micronaut.http.services.vectordb.url=http://localhost:9999
